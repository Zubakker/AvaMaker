# This file is a part of the application
# This preset doesn't do anything, it leaves the username the default color
# But it is still needed for the __modloader.py to work correctly


def main(USERNAME):
    colored = list()
    uncolored = list(range(len(USERNAME)))

    return colored, uncolored
